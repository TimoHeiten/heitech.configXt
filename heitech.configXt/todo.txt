
var configService = Resolve();

configService.Initialize(dictionary<string, object>)

configService.CreateAsync();
configService.RetrieveAsync();
configService.UpdateAsync();
configService.DeleteAsync();

string, object (jeweils) 
    primitive literals
    complex objects
    collections
returns bool 

store:
    concurrentdictionary in memory on end update file by overwrite

interface 
    app-connector (via zer0mq RqRep)

ui:
    blazor
    cli


